This is API for the my chat application, the API is not done on 100% but its already is ready for usage. there can be improved some of the controllers and models.

safety notes:
```
in this API I have not used BCrypt(password Hasher). so all passwords can be leaked.
(if some one will crack my Database)
```
